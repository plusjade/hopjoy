<div class="row">
  <h2 class="main-block highlight">
    <%= @language.name %> for <%= @environment.name %>
  </h2>
</div>

<ul>
	<li>What is <%= link_to @language.name, language_path(@language.slug) %>?</li>
	<li>What is <%= link_to @environment.name, environment_path(@environment.slug) %>?</li>
</ul>

<div class="row">
  <div class="span10 columns">
    <%= raw @language.content(@environment.slug) %>
  </div>
</div>

